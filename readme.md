This repo demonstrates how environment variables work with XUnit. 

## Prerequisites
- .NET Core 2.0

## Steps
- Clone repo
- run `build.ps1`. Test should fail
- uncomment the first line in 'build.ps1
- run `build.ps1`. Test now should pass

